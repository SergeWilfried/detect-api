# License Plate Detection API - Environment Variables
# Copy this file to .env and fill in your actual values

# YOLO Model Configuration
# Path to your custom YOLO model file (optional)
# YOLO_MODEL_PATH=./models/license_plate_detector.pt

# Detection Confidence Threshold (0.0 to 1.0)
# Lower values = more detections but potentially more false positives
# Higher values = fewer but more confident detections
CONFIDENCE_THRESHOLD=0.25

# OCR Engine Configuration
# Options: 'easyocr' or 'gemini'
# - easyocr: Local OCR using EasyOCR (default, no API key needed)
# - gemini: Google Gemini Vision API (requires GEMINI_API_KEY)
OCR_ENGINE=easyocr

# Google Gemini API Key (required if OCR_ENGINE=gemini)
# Get your API key from: https://makersuite.google.com/app/apikey
# GEMINI_API_KEY=your-gemini-api-key-here
